/** Record Object (funções para capturar objetos)
 * getField(options)
 * getValue(fieldName)
 * setValue(fieldname, value)                  context.newRecord
 * getText(options)
 * setText(options)
 */

/** Tipos e niveis de log   // executa o log pelo nível de importãncia (se for o mesmo, executa o primeiro)
 * log.Emergency (title, details) 1° nivel
 * log.Error (title, details) 2° nivel
 * log.Audit (title, details) 3° nivel
 * log.Debug (title, details) 4° nivel
 */  