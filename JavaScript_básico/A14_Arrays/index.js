let alunos = ['Rafael', 'Gabriel', 'João'];
// console.log(alunos.length); // retorna o tamanho da lista
// alunos[alunos.length] = 'Luiza'; // outra sintaxe para adicionar itens


                        /***************************************************************
                         * alunos.push('Otávio'); // é basicamente o .append do python *
                         ***************************************************************/
// alunos.unshift('Luiza'); // adiciona o item no começo da lista
// alunos.pop('João'); // remove o item da lista (se encontrado, é o último)
// const removido = alunos.pop('Gabriel'); // o item removido com o .pop pode ser realocado em uma variável
// console.log(`Item removido: ${removido}`); 

// const removido = alunos.shift('Gabriel'); // o item removido com o .pop pode ser realocado em uma variável
// console.log(`Item removido: ${removido}`); // remove o item da lista (se encontrado, é o primeiro)

// delete alunos[1]; // deleta o conteúdo do indice
// alunos.push('Maria')
// alunos.push('chesque')
// alunos.push('dale')
// console.log(alunos.slice(0,3 +1)); // fatia o conteúdo, (primeiroIndicte, últimoIndice) para mostrar o último, é só adicionar +1
// console.log(alunos.slice(-3));  // seleciona os últimos que foram criados 