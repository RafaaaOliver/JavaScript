const nome = 'luiz'; // string
const nome1 = "Luiz"; // string
const nome2 = `Luiz`; // string
const num1 = 10; // number
const num2 = 10.52; // number
let nomeAluno; // undefined = não aponta para local nenhum  na memória
let sobrenomeAluno = null; // Nulo -> não aponta para local nenhum na memória 

// undefined é colocado automáticamente
// o null pode ser setado pelo próprio programador

const aprovado = false; // Boolean = true , false (lógico)

// console.log(typeof) // typeof() pega o tipo da variável

console.log(typeof(sobrenomeAluno))