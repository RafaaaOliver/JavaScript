// constantes são variáveis imutáveis
// dentro de aspas = sting | fora = number
const nome = 'joão';
console.log(nome);

// nome = 'Rafael';  // vai dar erro pois o valor não pode mudar


/*******************************************
 * Não podemos redeclarar constantes com CONST
 * Case-sensitive
 * Não utiliza VAR, utilize LET
 * camelCase
 * precisa ser criada e inicializada
 *******************************************/

let estilo = 1234;

console.log(typeof(estilo)); // a função typeof() retorna se o 
                            // conteúdo da variável é string ou number



